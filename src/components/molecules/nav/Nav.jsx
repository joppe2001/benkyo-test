import Form from '../form/Form';
import styles from './Nav.module.scss';

const Nav = () => {
  return (
    <div className={styles.loginContainer}>
      <Form />
    </div>
  );
};

export default Nav;
