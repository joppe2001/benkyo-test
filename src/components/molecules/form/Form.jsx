import styles from './Form.module.scss';

const Form = () => {
  return (
    <div>
      <form className={styles.form}>
        <h2 className={styles.title}>Benkyo</h2>
      </form>
    </div>
  );
};

export default Form;
