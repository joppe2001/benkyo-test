const ServerGrid = () => {
    // this is a page where all the chatting serrvers will be displayed in a grid format
    return (
        <div>
            <h1>Server Grid</h1>
        </div>
    );
}

export default ServerGrid;